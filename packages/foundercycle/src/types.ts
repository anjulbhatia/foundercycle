/** Shared kernel types for the foundercycle package. UI keeps its own copy. */
export type CardType = "meeting" | "task" | "bug" | "idea" | "follow-up";

export type ProviderId = "gmail" | "calendar" | "notion" | "slack" | "github";

export type ColumnId = "planned" | "ongoing" | "completed";
